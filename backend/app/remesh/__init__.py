"""메시를 물려받지 않고 다시 짜기.

SUPREM 은 하나의 메시를 공정 내내 제자리에서 고쳐 쓴다. 국소 연산이 퇴화를
낳고 그것이 쌓이다가 panic 으로 끝난다 — 품질을 관리하는 주체가 없기 때문이다.

여기서는 현대 도구가 하는 대로 **기하 / 메시 / 물성값을 분리한다.** 기하는
`.str` 에서 그대로 뽑고, 메시는 품질 보장이 있는 생성기(gmsh)가 새로 짜고,
물성값은 옛 메시에서 새 메시로 이송한다.

`.str` 이 완전한 교환 형식이라 시뮬레이터 코드를 고치지 않고 이것이 가능하다.
"""
